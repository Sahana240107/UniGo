# Database

Place your existing Supabase schema in `database/migrations/001_initial_schema.sql`.

The current project only scaffolds the repo structure. When we wire the app to Supabase, this folder should contain the SQL already created for UniGo.

